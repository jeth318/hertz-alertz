const button =
  "btn btn-primary dark:hover:text-white dark:focus:text-white dark:active:text-red dark:bg-black dark:text-hertz dark:border-hertz";

const card = "card dark:bg-black bg-base-100 shadow-xl dark:text-black";

const select =
  "select select-bordered w-full max-w-md dark:bg-gray-900 dark:text-stone-300";

const label =
  "input input-bordered flex items-center gap-2 dark:bg-gray-900 dark:text-stone-300";

const classes = {
  button,
  card,
  select,
  label,
};
export default classes;
